# Журнал доработок ProtocolOOT

История изменений по версиям. Нумерация ведётся с **1.4**.  
Текущая версия в программе: **«Справка» → «О программе…»** (`APP_VERSION` в `ProtocolOHT_next/protocol_app_info.py`).

**Правило:** при каждом изменении `APP_VERSION` добавляйте раздел ниже с датой и перечнем доработок.

---

## 1.6.3 — июнь 2026

### Автообновление (этап 2)
- Вместе с **ProtocolOOT.exe** обновляется каталог **`data/`** (политика **replace**): шаблоны протоколов, инструкции, FAQ, журнал доработок, XSD Минтруд, icon.ico.
- **Корень** папки с exe (`protocols.db`, `Data_base.xlsx`, `Protokol/` и т.д.) **не затрагивается**.
- Перед заменой `data/` создаётся резервная копия **`data.backup/`**.
- В `manifest.json` — секция **`data_files`**; публикация: `tools/publish_update_manifest.py` копирует `data/` на шару.

### Версия
- В «О программе»: **1.6.3**.

---

## 1.6.2 — июнь 2026

### Автообновление
- **Сканирование вложенных каталогов** шары: `manifest.json` и `ProtocolOOT.exe` в папках версий (`windows/1.6.x/`).
- Выбор **новейшей** версии выше текущей, даже если корневой `manifest.json` устарел.
- Утилита **`tools/scan_update_share.py`** — просмотр всех версий на шаре.

### Версия
- В «О программе»: **1.6.2**.

---

## 1.6.0 — июнь 2026

### Автообновление (этап 1)
- При старте **собранного .exe** — проверка `manifest.json` на сетевой шаре (по умолчанию `\\SERVER\SOFT\ProtocolOOT\`).
- Меню **«Справка» → «Проверить обновления…»** — ручная проверка.
- Установка: копия нового `.exe` с проверкой **sha256** и размера, замена через `rename`.
- После обновления — диалог **«Что нового»** (`--show-changelog=`).
- Настройка: `update_config.json` рядом с программой; переменная `PROTOCOLOOT_UPDATE_MANIFEST`.
- Для отладки из исходников: `PROTOCOLOOT_UPDATE_CHECK=1`.
- Публикация манифеста: `tools/publish_update_manifest.py`; документация — `docs/UPDATES.md`.

### Версия
- В «О программе»: **1.6.0**.

---

## 1.5.1 — июнь 2026

Точка отката перед внедрением автообновления (тег `v1.5.1` в репозитории).

### Сборка и комплект
- `ЖУРНАЛ_ДОРАБОТОК.md` включён в Windows-сборку (`build_windows_exe.py`).
- Расширен архив для проверки ИБ (`tools/pack_ib_review.py`): тесты, `linux_port`, документация.

### Протоколы и V_PROF
- При **ручной подстановке должности** на титульном листе в матрицу V_PROF не подтягивается должность из Excel.

### Linux
- После **технического протокола** окно корректно возвращается из полноэкранного/развёрнутого режима.

### Версия
- В «О программе»: **1.5.1**.

---

## 1.5 — июнь 2026

### Журнал протоколов
- При повторном формировании протокола с тем же **№, датой и видом** запись в `protocols.db` **обновляется**, а не дублируется.
- При запуске программы **удаляются старые дубликаты** в журнале (остаётся запись с наибольшим `id` — актуальная).
- В окне **«Журнал протоколов»** и в окне **«Шаблон для сайта Минтруда»** в списке показывается **одна строка на протокол** (без повторов).

### Выгрузка Минтруд
- Excel/XML формируются **только из выбранных записей** журнала.
- Убрано автоматическое **слияние с уже сохранённым `.xlsx`** — оно приводило к лишним строкам при повторной выгрузке.
- Внутри одной выгрузки по-прежнему убираются дубли строк (один сотрудник × программа × протокол).

### Комиссия по проверке знаний
- **Именованные профили** комиссии (например «НПС и ЦТТ») — отдельно для охраны труда и для технических вопросов.
- Профили хранятся в таблице `commission_profiles` в `protocols.db`: приказ, место проверки, состав.
- В окне комиссии: выбор профиля из списка, **Загрузить**, **Сохранить** (создание/обновление), **Удалить профиль**.
- При первом запуске после обновления прежняя комиссия из настроек переносится в профиль «Основная» (или по названию подразделения).

### Интерфейс
- **Четыре цветовые схемы** (меню «Вид» и вкладка в настройках); выбор **сохраняется** в `protocols.db`.
- Перестроена главная форма: фильтр сверху, список сотрудников слева, «Участник» и «Реквизиты» справа, **«Программы обучения»** под реквизитами.
- Кнопки с **контрастным** фоном и рамкой; настройки разнесены по вкладкам («Файлы данных», «Шаблоны», «Прочее»).
- При **техническом протоколе** окно расширяется, список сотрудников сжимается; при снятии флага **возвращается обычный размер** окна и списка.
- Исправлена кнопка **«Загрузить из Excel»**; окна из меню открываются **модально** (не теряют фокус).

### Шаблоны и данные (bundle)
- Поддержка **`.odt`** (шаблоны протокола) и **`.ods`** (базы сотрудников/программ, шаблон Минтруда) из каталога `bundle/`.
- Модуль `bundle_integration.py`: поиск `.docx`/`.xlsx` или конвертация через **LibreOffice** (кэш в `.office_cache/`).
- В комплект добавлены: `default_protocol.odt`, `default_protocol_tehnicheskiy.odt`, `Data_base.ods`, `Programs_base.ods`, `Шаблон_Минтруд_XSD_УМН.ods`.
- Выгрузка **«Восстановление данных»** копирует ODT/ODS наряду с DOCX/XLSX.

### Сборка Linux
- Комплект **`ProtocolOOT_linux_build/`** для сборки без git (`tools/pack_linux_build.py`).
- Скрипты **`check_env.sh`**, **`install_deps.sh`**, **`build.sh`**; порядок: сначала зависимости, затем проверка.
- **ALT Linux**: для сборки — `python3.11-dev` (не `python3.11-devel`), виртуальный дисплей — **`xorg-xvfb`** (не `xvfb`); `libpython3.11` обязателен.
- Ветка **`linux`** синхронизируется с `main` (`tools/sync_linux_branch.py`); проверка ruff как на Windows (`ruff.toml` в комплекте).
- `./build.sh --no-verify` — сборка без verify/ruff (если окружение уже проверено).

### Прочее
- Добавлены unit-тесты: журнал (upsert, очистка дублей), профили комиссии, dedupe строк выгрузки, **UI/тема**, **bundle**, **Linux-сборка**.
- Меню **«Справка» → «Журнал доработок…»**; файл `ЖУРНАЛ_ДОРАБОТОК.md` в комплекте.
- При первом запуске после обновления — сообщение об удалении старых дублей журнала (один раз на версию).
- Партия «по одному на каждого» обновляет журнал без предварительного удаления строк.
- Версия в «О программе»: **1.5**.

---

## 1.4 — июнь 2026

Базовая зафиксированная версия, с которой ведётся этот журнал.

### Документация
- **Подробная инструкция** пользователя (`ПОДРОБНАЯ_ИНСТРУКЦИЯ_для_пользователя.md` / `.docx`) с **19 скриншотами** интерфейса.
- Скрипты: `tools/capture_manual_screenshots.py`, `tools/instruction_md_to_docx.py`, `generate_podrobnaya_instruction_docx.bat`.

### Минтруд и протоколы
- Выгрузка для реестра Минтруда: программа **«В»** — должности и программы из **метаданных протокола** (снимок при сохранении в журнал).
- Параллельно сохраняются **`.xlsx` и `.xml`** с одними данными.
- Меню **«Минтруд»**: реквизиты работодателя, выбор записей из журнала для шаблона Excel.

### Интерфейс и шаблоны
- Обновлены шаблоны `default_protocol.docx` и `default_protocol_tehnicheskiy.docx`.
- Окно **«О программе»**: кликабельный **mailto** с темой письма для обратной связи.
- С **сформированных DOCX** снимается защита редактирования (удобнее править в Word).

### Сборка и перенос
- **Linux**: CI-сборка PyInstaller, ветка `linux`, `tools/sync_linux_branch.py`.
- Обновление **эталона** переносимой сборки (`tools/update_etalon.py`).

### Версия
- В «О программе»: **1.4**.
