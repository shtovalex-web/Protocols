# Проверка перед коммитом: py -m ruff check .
# Ловит в т.ч. обращения к несуществующим именам (частая причина — забытый импорт из protocol_docx).
[lint]
select = ["E9", "F821", "F822", "F823"]

[lint.per-file-ignores]
# Импорты после sys.path.insert — намеренно.
"main.py" = ["E402"]
"app/main.py" = ["E402"]
